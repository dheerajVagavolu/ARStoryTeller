* The Code X file is the New Language file in which the language is written in the specified Format.
* The trans.cpp file reads "code.x" and creates a file "output.py"
* "output.py" file is the one which goes into the blender API to generate the 3-D models according to the language.